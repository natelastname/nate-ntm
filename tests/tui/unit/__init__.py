"""Unit tests for the Textual runtime console.

This package contains tests for the TUI-facing components such as the
:class:`ConsoleApp`, screens, and widgets. These tests are intentionally kept
lightweight so they can run in headless environments without requiring an
interactive terminal.
"""
